interface Props {
    container: HTMLElement;
    allExpanded: boolean;
    onChange: (expand: boolean) => void;
}
export declare function renderGlobalParentToggleButtons({ container, allExpanded, onChange }: Props): void;
export {};
